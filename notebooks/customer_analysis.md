# Customer Analysis Notebook

This folder will contain the exported Databricks notebook for the customer transaction analysis mini project.

Planned contents:
- Reading sample tables from Databricks catalog
- Basic exploration with `printSchema()`, `show()`, and `display()`
- Transformations with `withColumn()`
- Joins between customer and transaction tables
- Aggregations and ranking with window functions
